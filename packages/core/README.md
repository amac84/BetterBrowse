# @betterbrowse/core

Core BetterBrowse auditing engine used by the `betterbrowse` CLI.

This package is published so `betterbrowse` installs cleanly from npm.

Most users should install the CLI package:

```bash
npm install --save-dev betterbrowse
```
